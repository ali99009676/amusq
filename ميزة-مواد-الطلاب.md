# مواد الطلاب + الكوينز + تجربة العشر دقائق

**الفحوص: ٦٠٢/٦٠٢ ناجح** · و**٤٠ فحص قاعدة بيانات** على PostgreSQL 18 حقيقي (٢٣ للأمان والحدود + ١٧ لرحلة كاملة من الرفع إلى وصول الكوينز).

---

## أولًا: خطوة واحدة مطلوبة منك

شغّل `db/UGC-COINS.sql` في **SQL Editor** على Supabase، ثم **Integrations → Data API → Reload schema**.

الملف آمن للتكرار تمامًا: لا يحذف عمودًا ولا جدولًا، وتشغيله مرتين لا يغيّر شيئًا. (ولو لم تشغّل `ADMIN-DASHBOARD.sql` و`SETTINGS-UPGRADE.sql` من قبل، شغّلهما أيضًا.)

ثم ارفع على GitHub: `index.html` ومجلدات `src` و`db` و`api`.

---

## قرار خالفتُ فيه ظاهر طلبك — وأريد رأيك

طلبت نمط `enhanced` يعيد صياغة الأسئلة. هذا يصطدم بقاعدة القداسة، وهي أهم قاعدة في المشروع. **نفّذته، لكن بثلاثة قيود لم تطلبها:**

**١. الأصل لا يُمحى أبدًا.** أضفت عمودَي `q_original` و`options_original`. في النمطين معًا يُحفظ ما وصل من الملف حرفًا بحرف. المحسَّن هو المعروض فقط، والتراجع ممكن دائمًا بلا إعادة رفع.

**٢. إعادة ترتيب الخيارات ممنوعة منعًا باتًا.** هذا أخطر ما في الميزة: موضع الإجابة **رقم** لا نص. لو أعاد النموذج ترتيب الخيارات لبقي الرقم كما هو وأشار إلى **إجابة خاطئة**، وصار الطالب يحفظ الخطأ ولا يدري. الفحص الآلي يُجرّب هذا الهجوم فعلًا ويثبت أن الإجابة تبقى سليمة.

**٣. التحسين يُقبل خيارًا خيارًا لا كتلة واحدة.** كل خيار محسَّن يُقارن بأصله بعد التطبيع؛ التنظيف الإملائي يمرّ (`N-acetylcysteine` ← `N-Acetylcysteine (NAC)`)، واستبدال خيار بآخر يُرفض ويبقى الأصل.

`strict` هو الافتراضي، وأي قيمة مجهولة تسقط إليه. **إن أردت منع `enhanced` كليًا قل لي وأزيله في دقائق.**

---

## كيف تعمل الميزة

### الطالب يرفع مادته
`#/admin/upload` صار مفتوحًا لكل مسجَّل لا للمشرف وحده. يكتب اسم المادة، ويختار النمط من بطاقتين مشروحتين بالعربي، ويرفع ملفه. بعد النشر تظهر شاشة فيها **رابطه جاهزًا للنسخ**:

```
https://amsuq.alsoqoor.com/#s/اسم-المادة-a1b2c3?ref=USER_ID
```

الموجّه يقبل `#s/slug` و`#/s/slug` معًا، فالرابط يبقى قصيرًا كما يُشارَك في الواتساب — ويعمل بفتح الملف محليًا.

### تجربة العشر دقائق — للمنشئ وحده
- **الوقت يُحسب في الخادم لا في المتصفح.** المتصفح يرسل نبضة كل ٣٠ ثانية، والقاعدة تجمعها وتقصّها عند ٦٠٠ ثانية.
- جرّبت الغش في الفحص: نبضة بقيمة ٩٩٩٩٩ ثانية **تُقصّ إلى ٦٠**، ونبضة سالبة **لا تُرجع العدّاد للوراء**، وفتح خمس ألسنة لا يغيّر المجموع.
- شريط علوي يعرض الوقت، ويصير أحمر في آخر دقيقتين — إنذار قبل القفل لا مفاجأة.
- عند الصفر: قفل فوري وشاشة شراء.

### الزميل لا تجربة له
من وصله الرابط يرى **صفحة تعريفية** بالمادة وعدد أسئلتها وسعرها وزر الشراء — ولا يُنشأ له سجل تجربة أصلًا. الفحص يثبت أنه لا يرى شريط عدّاد إطلاقًا.

### الكوينز
٥٠ كوين للمنشئ عن كل عملية شراء عبر رابطه، **بعد تأكيد الدفعة لا قبلها**، وثلاثة حراس:

| المحاولة | النتيجة |
|---|---|
| شراء مكرر لنفس المادة من نفس الشخص | فهرس فريد يمنع تكرار المكافأة |
| رابط إحالة مزوّر باسم شخص آخر | مرفوض — القاعدة تتحقق أن المُحيل هو المنشئ فعلًا |
| المنشئ يشتري مادته بنفسه | لا كوينز |

ودالة المنح ممنوحة لـ `service_role` وحده، وصلاحية العموم منزوعة صراحة. **المتصفح لا يعرف اسم الدالة أصلًا** — فحص آلي يتأكد من ذلك في كل بناء.

---

## لوحتك ومحفظة الطالب

**تبويب «مواد الطلاب»** في لوحة التحكم: من رفع كل مادة، وبأي نمط (موسوم بلون)، وكم بيعت، وكم كوينز مُنحت، وكم اختبارًا جرى عليها. تسعّر (السعر يُقصّ إلى ٥٠٠٠ ريال في الواجهة والقاعدة معًا)، وتوقف مادة مخالفة، وتحذفها بتأكيدين. والمشرف يُنبَّه بعدد ما لم يُسعَّر بعد.

**محفظة الطالب** في `#/account`: رصيده، ومبيعاته، ومواده مع رابط نسخ لكل واحدة، وسجل الكوينز مع سبب كل حركة. مادته بلا سعر تُوسم «بانتظار التسعير» بدل أن تُترك صامتة.

---

## ملاحظتان

**الفحوص السابقة ٤٩٢ لا ٣٧٦** — الرقم الذي ذكرته قديم؛ نمت اللوحة والمحرر والإعدادات بينهما. الآن ٦٠٢، ولم يُكسر فحص واحد.

**أصلحت خللًا كان سيؤذي طلابًا يدفعون:** بوابة المادة كانت ستُقفل عند أي تعثّر شبكة. الآن انقطاع الاتصال يعني «لا أعرف» لا «ممنوع» — فمن اشترى يذاكر في الطائرة، والقاعدة تحرس القراءة الفعلية على كل حال.
