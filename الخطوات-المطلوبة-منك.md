# ما ينقص لتشتغل اللوحة الجديدة — ثلاث خطوات

## ١ · شغّل ملفَّي SQL على Supabase
افتح **SQL Editor** في لوحة Supabase، والصق كل ملف على حدة ثم **Run**:

| الملف | ماذا يضيف |
|---|---|
| `db/ADMIN-DASHBOARD.sql` | دوال اللوحة: المؤشرات، السلسلة اليومية، شرائح النتائج، أداء المواد، آخر النشاط |
| `db/SETTINGS-UPGRADE.sql` | ١٤ إعدادًا جديدًا + دالة التصدير + فحص صحة المحتوى |

الملفان **آمنان للتكرار** — لو شغّلتهما مرتين لن يُحذف شيء ولن تتغيّر قيمة موجودة.

بعدهما، من **Integrations → Data API → Settings** اضغط **Reload schema** كي يرى REST الدوال الجديدة.

---

## ٢ · احفظ Site URL في Supabase (ما زالت معلّقة من قبل)
**Authentication → URL Configuration → Site URL** =
```
https://amsuq.alsoqoor.com
```
ثم **Save**. بدونها يصل بريد الدخول لكنه لا يُدخل.

---

## ٣ · ارفع الملفات على GitHub
افتح: https://github.com/ali99009676/amusq/upload/main

اسحب من مجلد `amusq-deploy`:

- `index.html`  ← **الأهم، هذا هو التطبيق كله**
- مجلد `src` كاملًا
- مجلد `db` كاملًا

ثم **Commit changes**. Vercel ينشر خلال دقيقة.

---

# ما الجديد في اللوحة

## تبويب «اللوحة» (الافتراضي الآن)
- **٦ مؤشرات حيّة**: الطلاب · المتصلون الآن · الاختبارات · متوسط النتائج · الأسئلة · المواد المنشورة
- **رسم النشاط اليومي** — عمود لكل يوم، وخط برتقالي لمتوسط النتيجة فوقه. مدى ٧ / ١٤ / ٣٠ يومًا
- **توزيع النتائج** على خمس شرائح بالألوان
- **أداء المواد** مرتّبًا بالأكثر استخدامًا — اضغط أي مادة لتفتح محررها
- **آخر ١٢ نشاطًا** بالوقت النسبي

كل الرسوم مبنية بيدنا (SVG) — لا مكتبة رسم واحدة، تمشيًا مع قاعدة المشروع.

## محرر المادة (اضغط أي مادة)
- **الهوية**: الاسم، الوصف، ٦ ألوان، ١٠ أيقونات، الترتيب، موعد الاختبار، النشر، المجانية
- **المحاور**: إضافة وترتيب وحذف — ومحور عليه أسئلة **لا يُحذف** حتى تنقلها
- **متصفح الأسئلة**: بحث في النص والخيارات، تصفية بالمحور / المهم / الإجابة المستنتجة / بلا شرح
- لكل سؤال: تصحيح الإجابة **بالضغط على الخيار الصحيح**، تعليم المهم، تحرير الشرح والترجمة وبطاقة الحفظ، الحذف بتأكيدين

> **نصّ السؤال وخياراته لا يُحرَّران هنا — عمدًا.** قاعدة القداسة تقول إن كلام الدكتور يبقى حرفًا بحرف؛ الخطأ في النص يُصلَح برفع الملف من جديد لا بالكتابة فوقه. لا يوجد حقل إدخال عليهما أصلًا، والفحص الآلي يتأكد من ذلك في كل بناء.

## الإعدادات — ثلاث مجموعات
- **هوية المنصة**: الاسم، الشعار، الترحيب، بريد الدعم، واتساب
- **افتراضات الاختبار**: عدد الأسئلة، المدة، علامة النجاح، خلط الأسئلة، خلط الخيارات، التصحيح الفوري
- **الوصول**: فتح التسجيل، لوحة المتصدرين، حد الأجهزة، أيام التجربة، وضع الصيانة ورسالته

ولها ثلاث خصائص:
- **الحفظ لكل مجموعة وحدها**، ولا يُرسل إلا الحقل الذي تغيّر فعلًا
- **الأرقام تُقصّ إلى مداها** قبل المغادرة، والقاعدة نفسها ترفض الخارج (قيد `settings_sane`)
- **صحة المحتوى**: يعرض ما ينقص قبل أن يراه الطالب — إجابة خارج نطاق الخيارات، سؤال بلا شرح، مادة فارغة…
- **تصدير نسخة كاملة JSON**: كل المواد والأسئلة والإعدادات بيدك

---

**الفحوص: ٤٩٢ / ٤٩٢ ناجح.** ودوال SQL تحققت على PostgreSQL 18 حقيقي — بما فيها اختبار فساد مفتعل تأكدنا أنه يُكشف.
